# Claude Code 项目指令：LeadRadar 数字包装商机雷达

你正在开发一个垂直 B2B 销售情报系统：LeadRadar。请先理解 docs/ 下的交接资料，再实施代码。

## 项目目标

帮助用户从公开信息中发现食品、农产品、区域品牌、包装/印刷/检测/代运营相关商机，重点识别“预算正在发生”的线索，并生成可电话跟进的高质量列表。

## MVP 范围

第一版只做：
- 政府采购/公共资源公告的关键词发现与采集。
- 网页正文/PDF 文本抽取。
- LLM 结构化抽取：项目名称、采购单位、预算、时间、联系人、关键词、产品包匹配、证据片段。
- 线索评分：预算强度、场景匹配、时间窗口、可触达性、成交杠杆。
- 线索列表、详情、证据、电话话术、跟进状态、导出。

暂不做：自动拨号、自动群发、自动加微信、个人手机号抓取、绕过验证码/登录/反爬、全量社媒爬取。

## 核心约束

- 所有 AI 生成结论必须保存 evidence_text、source_url、confidence。
- 不允许模型编造电话、联系人、预算金额、公告链接。
- 只处理公开企业/机构业务信息；拒绝联系对象必须进入 blocklist。
- 爬虫必须限速、尊重 robots 与站点条款；优先 API 或公开 HTML。
- 测试中不得访问真实外网；使用 fixtures 或 mocks。
- 代码应保持模块化：采集、抽取、评分、线索服务、API 分离。

## 推荐开发顺序

1. 数据模型和配置加载。
2. 评分模型与测试。
3. LLM 抽取 schema 和 mock provider。
4. 采集器接口与示例 provider。
5. 线索服务：raw document → signal → lead。
6. FastAPI CRUD/API。
7. 导出与跟进状态。
8. 最小后台页面或 API 文档。

## 代码风格

- Python 使用类型注解。
- 核心逻辑必须有单元测试。
- 外部 API 调用封装成 Provider 接口，便于替换。
- 配置走环境变量和 YAML，不硬编码密钥。
- 任何新依赖必须说明用途。

## 常用命令

```bash
make setup
make test
make lint
make dev
```

若命令不可用，先修复 Makefile/pyproject，再继续开发。
