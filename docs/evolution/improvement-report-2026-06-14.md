# Product Evolution Report — 2026-06-14

> 项目：LeadRadar（数字包装商机雷达）
> 扫描范围：后端 FastAPI + 前端 Next.js + 产品文档
> 扫描日期：2026-06-14
> 雷达状态：首次扫描，新建雷达

---

## 执行摘要

本次扫描从 **PRD 差距、用户旅程断点、功能闭环、功能深度** 4 个维度出发，共识别出 **17 条产品功能改进方向**，聚类为 **5 个改进包**。所有候选项均经过代码/页面实质内容交叉验证，未发现已 secretly 实现的隐藏功能。

| 排名 | 改进包 | 包 RICE | 关键价值 |
|---|---:|---:|---|
| 1 | 销售跟进闭环深化 | **20.2** | 直接提升转化率，补齐电话工作台缺失的提醒与原因沉淀 |
| 2 | 线索池搜索与筛选增强 | **15.1** | 销售每天最高频页面，降低线索查找成本 |
| 3 | 系统运营能力 | **10.8** | 让手动任务可管可控，为自动化调度打基础 |
| 4 | 配置管理中心完善 | **9.1** | 释放管理员自配置能力，减少代码变更 |
| 5 | 数据质量与复盘分析 | **5.9** | 支撑 US-005 复盘诉求，提升线索可信度 |

---

## 改进包详情

### 包 1：销售跟进闭环深化（RICE = 20.2）

**一句话**：把"跟进记录"从备注升级为带下次提醒、原因分类、黑名单的完整销售闭环。

| # | 改进方向 | 状态 | RICE | 证据链 |
|---|---|---|---|---|
| 1.1 | 跟进表单支持设置「下次跟进时间」 | new | 16.8 | `FollowUp.next_action_at` 已在模型定义，但 `web/src/app/workbench/[id]/page.tsx` 的表单未暴露该字段；API `POST /leads/{id}/follow-ups` 已接收 `next_action_at` |
| 1.2 | 增加「到期跟进」提醒入口 | new | 9.5 | 后端 `GET /follow-ups/due` 已存在，但侧边栏/工作台无入口，销售无法主动查看 |
| 1.3 | 通话结果增加原因分类（预算不足/无需求/联系不上/已合作等） | new | 7.0 | 当前 `CALL_RESULTS` 仅展示结果文本，无结构化原因；影响复盘统计 |
| 1.4 | 黑名单/屏蔽联系人或机构 | new | 5.3 | `Blocklist` 模型已定义，但无 API 路由与前端入口 |

**建议 PRD 方向**：
- 外呼工作台 V2：下次跟进时间选择器、到期跟进通知气泡、拒绝原因枚举、一键屏蔽

**调用建议**：`/prd-generator 销售跟进闭环深化`

---

### 包 2：线索池搜索与筛选增强（RICE = 15.1）

**一句话**：让销售能在海量线索中按地区、预算、时间窗口和关键词快速定位目标客户。

| # | 改进方向 | 状态 | RICE | 证据链 |
|---|---|---|---|---|
| 2.1 | 线索池增加全局搜索框 | new | 12.6 | `web/src/components/leads/lead-filters.tsx` 当前无搜索输入；`Lead`/`Organization` 模型已含 name/title |
| 2.2 | 筛选支持地区、预算金额、发布时间窗口 | new | 10.1 | PRD US-001 明确要求按地区、预算金额筛选；当前 `GET /leads` 仅支持 grade/status/signal_type |
| 2.3 | 批量操作（批量导出、批量状态更新） | new | 5.0 | 当前 `LeadTable` 无多选列；导出仅支持按 grade 全量导出 |

**建议 PRD 方向**：
- 线索池高级筛选与批量操作：搜索、预算区间、省份、发布时间、多选批量流转

**调用建议**：`/prd-generator 线索池搜索与筛选增强`

---

### 包 3：系统运营能力（RICE = 10.8）

**一句话**：让手动生成任务可取消、可重试，并为定时自动扫描做准备。

| # | 改进方向 | 状态 | RICE | 证据链 |
|---|---|---|---|---|
| 3.1 | 手动任务支持取消/重试 | new | 9.0 | `ManualTaskStatus` 枚举含 pending/running/completed/failed/partial_failed，但 API 无取消/重试端点；详情页无操作按钮 |
| 3.2 | 自动定时扫描任务调度 | new | 4.2 | PRD 提到"扫描频率"配置，当前仅支持手动触发 `POST /manual-tasks` 和 `POST /crawl/trigger`；无 scheduler/cron 模块 |
| 3.3 | 用户注册/用户管理入口 | new | 2.8 | `web/src/app/login/page.tsx` 显示"联系管理员创建账号"；`auth_routes.py` 有 register 接口但前端无入口 |

**建议 PRD 方向**：
- 任务运营中心：任务生命周期管理 + 定时扫描调度器 + 用户管理入口

**调用建议**：`/prd-generator 系统运营能力`

---

### 包 4：配置管理中心完善（RICE = 9.1）

**一句话**：把配置页从只读展示升级为管理员可在线维护的数据中心。

| # | 改进方向 | 状态 | RICE | 证据链 |
|---|---|---|---|---|
| 4.1 | 数据源在线启用/禁用/编辑 | new | 7.6 | `SourcesTab` 仅展示列表；`Source` 模型含 enabled 等字段但无 PUT/DELETE API |
| 4.2 | 关键词组在线增删改 | new | 6.8 | PRD US-004 要求配置关键词组；当前仅读取 `data/keywords.yml` 展示 |
| 4.3 | 产品包在线增删改 | new | 4.7 | `PackagesTab` 只读；产品包当前硬编码在 `routes.py` 中 |
| 4.4 | 话术模板管理与预览 | new | 3.5 | `ScriptsTab` 仅为占位提示；`call_script.py` 已根据信号生成话术，但无模板管理 |

**建议 PRD 方向**：
- 配置中心 V2：数据源/关键词/产品包/话术模板的全量 CRUD

**调用建议**：`/prd-generator 配置管理中心完善`

---

### 包 5：数据质量与复盘分析（RICE = 5.9）

**一句话**：通过重复合并、来源复盘和漏斗分析，提升线索可信度和销售复盘效率。

| # | 改进方向 | 状态 | RICE | 证据链 |
|---|---|---|---|---|
| 5.1 | 重复线索合并与去重展示 | new | 4.9 | 后端 `services/dedup.py` 已基于 content_hash 去重；但前端无重复提示/合并操作入口 |
| 5.2 | 按来源/关键词/信号类型的复盘统计 | new | 4.7 | PRD US-005 明确要求；当前 `WeeklyReport` 仅汇总数量，无维度下钻 |
| 5.3 | 销售转化率漏斗可视化 | new | 3.9 | 状态流已定义（new → ... → won/lost），但 dashboard/report 无漏斗图 |

**建议 PRD 方向**：
- 数据质量中心：重复线索合并 + 来源复盘 + 转化漏斗

**调用建议**：`/prd-generator 数据质量与复盘分析`

---

## 维度统计

| 维度 | 发现数 | 主要集中区域 |
|---|---|---|
| PRD 差距分析 | 5 | 配置、复盘、筛选 |
| 用户旅程断点 | 5 | 工作台、配置页、侧边栏 |
| 功能闭环 | 5 | Source/ManualTask/FollowUp/Blocklist/Contact |
| 功能深度 | 2 | 筛选维度、批量操作 |
| 新机会 | 0 | 本次未启用 `--discover` |

---

## 已验证已实现的 PRD 功能

以下功能已确认存在且具备实质业务逻辑，不再作为改进项：

- ✅ 线索列表与详情展示
- ✅ 五维评分与等级
- ✅ 电话开场白与诊断问题
- ✅ 跟进记录创建
- ✅ CSV/Excel 导出
- ✅ 手动任务生成
- ✅ 基础仪表盘与周报
- ✅ JWT 登录

---

## 推荐下一步

按 RICE 优先级，建议依次进入 `/prd-generator`：

1. **销售跟进闭环深化**（最高 RICE，直接影响转化）
2. **线索池搜索与筛选增强**（销售高频页面）
3. **系统运营能力**（为自动化打基础）

---

*本报告由 product-evolution skill 自动生成。*
